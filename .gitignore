# Datos runtime del plugin (nunca se versionan).
uploads/
marcos/
output_*.pdf
tests/salida_*.pdf
tests/*.tmp

# El modulo modules/textmuy/ SE versiona en este repositorio (v4.2).

# Configuraciones de AI Assistant (Speckit recomendaciones).
.clinerules/
.specify/
