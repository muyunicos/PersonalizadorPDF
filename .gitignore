# Datos runtime del plugin (nunca se versionan).
uploads/
marcos/
output_*.pdf
tests/salida_*.pdf
tests/*.tmp

# Modulos autocontenidos: NO se versionan, se importan a mano tras cada
# actualizacion del modulo (ver modules/LEEME.md).
modules/textmuy/

